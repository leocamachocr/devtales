---
import Layout from './Layout.astro';

const { 
  title, 
  description,
  image,
  imageAlt,
  author = "Leonardo Camacho"
} = Astro.props;
---

<Layout 
  title={title}
  description={description}
  image={image}
  imageAlt={imageAlt}
  author={author}
  type="article"
>
  <div class="posts-page">
    <!-- Specific styles for posts -->
    <link rel="stylesheet" href="/devtales/styles/posts.css" slot="head" />
    
    <slot />
  </div>
</Layout>
